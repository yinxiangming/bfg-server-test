[pytest]
markers =
    api_integration: HTTP API integration tests (require BASE_URL and env credentials)

# Add project root to Python path so client_remote.py is importable from any subdirectory
pythonpath = .
